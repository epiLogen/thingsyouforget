import React from 'react';
import './Header.css';

const header = () => {
    return (
        <header className="App-header">
            <div className="logo">things you forget...</div>
        </header>
    );
};

export default header;